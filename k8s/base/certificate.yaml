apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: myapp-cert
spec:
  secretName: myapp-tls
  dnsNames:
    - myapp.local
  issuerRef:
    kind: ClusterIssuer
    name: selfsigned-issuer
